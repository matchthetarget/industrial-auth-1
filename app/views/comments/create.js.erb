$("<%= j (render 'comment', comment: @comment) %>").appendTo('#<%= dom_id(@comment.photo) %> > ul');
$("<%= j (render 'new_city') %>").appendTo('#<%= dom_id(@comment.photo) %> > ul');
$("#new_city").focus();
